# Copy to .env. Paper mode (the default) needs no keys: it reads public market data.
PAPER_MODE=true

# Only used when PAPER_MODE=false (real orders, real money).
# Use real Binance keys with trading enabled and withdrawals disabled.
BINANCE_API_KEY=
BINANCE_SECRET=
